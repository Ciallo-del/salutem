# 仓库规范（jianyou-health-cloud）

## 后端定时任务规范

- 后续新增或重构定时器、定时任务、周期扫描、延迟执行功能时，必须遵循 `BACKEND_TIMER_TASK_SPEC.md`。
- 任务创建、编辑、启用、停用、删除等管理功能放在对应业务模块中实现。
- 任务实际执行逻辑统一放在 `timer` 模块下实现，并优先实现 `JobRpcService#execute(String parameter)`。
- 业务模块不得新增 `@Scheduled`、Quartz `Job`、循环线程或启动即执行的定时扫描逻辑。

## org 模块构建约定

- 涉及 `common` 公共类新增、重命名、迁移或方法签名变更时，默认在仓库根目录执行联编：
  - `mvn -pl org -am -DskipTests compile`
- 若必须单独编译 `org` 模块，必须先安装最新 `common` 快照再编译：
  - `mvn -pl common -DskipTests install`
  - `mvn -pl org -DskipTests compile`
- 禁止在跨模块公共类变更后直接执行“只编译业务模块”并据此判断代码失败。
- 推荐使用脚本统一执行：
  - `scripts/org-build-reactor.ps1`（联编，默认流程）
  - `scripts/org-build-standalone.ps1`（单模块兜底流程）

## api 模块规范

- `api` 模块获取 token 统一使用 `getAccessToken` 方法。



# 项目开发规范（所有前端项目）

## 样式一致性原则

- 新页面与改造页面应优先复用项目已有组件、布局模式、样式变量与交互模式，确保视觉与交互尽可能贴近项目原有样式。
- 无独立设计稿时，参照同模块、同层级邻近页面实现，不得引入与项目整体风格明显脱节的新样式体系。
- 具体自查项见下方「UI 还原度自查要求」。

## UI 还原度自查要求

研发人员在开发、修改或验收页面时，应对照设计稿完成以下自查，确保页面样式尽可能贴近项目原有样式和设计要求。

### 页面层级一致性

- 二级页标题需与一级功能名称一致。
- 页面跳转路径和返回逻辑需符合设计要求。
- 多级弹窗、浮层的层级关系需符合设计规则。

### 安全区域适配

- 刘海屏、挖孔屏、灵动岛区域内容不得被遮挡。
- 底部手势操作条区域不得出现内容溢出。
- 页面内容需避开系统安全区。
- 异形屏适配后布局不得错位。

### 间距边距规范性

- 页面全局边距、同区域组件间距、文本行高、字间距需严格对齐设计稿数值。
- 不同模块的分割间距需符合设计规范。
- 避免出现间距忽大忽小的问题。

### UI 细节还原度

- 组件长宽、圆角、阴影参数需与设计稿一致。
- 所有元素对齐方式，包括左对齐、居中、右对齐，需与设计稿一致。
- 背景色、文字色、边框色等色值需使用设计规范标注的色号，不得出现肉眼可见色差。
- 图标裁切、矢量图清晰度需符合设计要求，不得拉伸变形。

### 热区交互匹配度

- 可点击元素的交互热区范围需符合移动端设计规范，最小不小于 48dp。
- 交互热区需和视觉展示区域一致。
- 热区不得超出或小于组件视觉范围。
- 相邻组件热区不得重叠，不得存在响应盲区。

### 组件状态完整性

- 排查组件可用、禁用、点击、加载、报错等全状态显示是否正确。
- 禁用状态的拦截提示文案、交互反馈、灰阶或透明度需符合设计要求。

### 字体适配合理性

- 字体类型、字重、字号需严格按照设计稿实现。
- 多语言、超大字号场景下文本不得截断、错位。
- 适配用户系统字体缩放后的展示效果需符合预期。

### 特殊场景适配

- 弱网、空数据、权限关闭、权限申请等边界场景下，占位图、提示文案、异常页面样式需和设计稿一致，不得样式错乱。
- 内容超长滚动时，吸顶、吸底悬浮栏定位需正确。
- 虚拟键盘弹出、收起后布局重绘需正常，内容不得被键盘遮挡。

### 多设备适配性

- 不同尺寸、不同分辨率屏幕下，布局不得错位，元素不得溢出或挤压。
- 需适配横竖屏切换场景。
- 图片列表、宫格布局的响应断点展示需符合设计要求。
- 折叠屏展开、折叠切换后布局刷新需正常。

### 动效还原准确度

- 页面转场动画、侧滑返回、下拉刷新、组件交互动效的时长、缓动曲线、动效轨迹需和设计标注一致。
- 不得出现卡顿、掉帧。
- 动效触发逻辑需符合设计要求。

### 触控反馈还原度

- 点击后的水波纹、高亮反馈效果需和设计一致。
- 反馈时长需符合交互规范。
- 不得出现反馈延迟、重复触发问题。



# 项目开发规范（所有项目）

## 文件编码与中文字符串规范

- **编码统一**：源码、配置、文档、脚本等文件的读写编码一律使用 UTF-8；禁止新增或改用 GBK、GB2312 等编码。
- **运行时配置**：HTTP 请求/响应、数据库连接、日志、邮件、文件 I/O 等场景的 charset/encoding 须配置为 UTF-8，不得使用 GBK。
- **乱码修复**：开发或 review 过程中发现文件乱码（如中文变 `???`、注释/文案不可读、编码与 UTF-8 不一致）时，须在当次改动中立即修复，不得遗留或叠加新乱码。
- **中文字符串**：代码中的中文文案、注释、提示信息优先直接写中文字符串，避免 `\uXXXX` 等 Unicode 转义及不必要的 HTML/XML 实体转义；仅在编译器、协议或框架强制要求时使用转义。

## 子 Agent 授权规则（显式授权）

当用户请求包含以下意图时，主 Agent 被授权使用子 Agent（spawn_agent）：

- “请指派/委派 agent”
- “执行和验收分开执行”
- “让另一个 agent 做验收/复测”

授权范围：

1. 允许主 Agent 为“执行”和“验收”分别创建独立子 Agent。
2. 验收 Agent 仅可执行只读与验证操作（页面检查、接口校验、日志核对、测试执行），不得修改业务代码。
3. 执行 Agent 完成后，必须由不同 Agent 执行验收并输出独立结论。

默认策略：

- 若用户未反对，满足上述触发条件时可直接创建子 Agent，无需再次口头确认。
- 若任务涉及高风险操作（删除数据、重置仓库、生产环境变更），仍需单独征得用户确认。
- 对所有缺陷修复任务，默认启用“执行 Agent + 验收 Agent”双 Agent 流程
