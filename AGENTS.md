## 子 Agent 授权规则（显式授权）

当用户请求包含以下意图时，主 Agent 被授权使用子 Agent（spawn_agent）：
- “请指派/委派 agent”
- “执行和验收分开执行”
- “让另一个 agent 做验收/复测”

授权范围：
1. 允许主 Agent 为“执行”和“验收”分别创建独立子 Agent。
2. 验收 Agent 仅可执行只读与验证操作（页面检查、接口校验、日志核对、测试执行），不得修改业务代码。
3. 执行 Agent 完成后，必须由不同 Agent 执行验收并输出独立结论。

默认策略：
- 若用户未反对，满足上述触发条件时可直接创建子 Agent，无需再次口头确认。
- 若任务涉及高风险操作（删除数据、重置仓库、生产环境变更），仍需单独征得用户确认。
- 对所有缺陷修复任务，默认启用“执行 Agent + 验收 Agent”双 Agent 流程
